자바스크립트에서도 배열을 사용할 수 있는데 선언 방법만 다르다. 
var 변수명 = new Array(크기);로 사용한다.
데이터 대입 출력은 자바와 동일하다.
*데이터 데입중 또 다른 명령어로는 배열이름.push(담을 데이터);기능이 있다.



var 변수명 = [];로도 선언이 가능하다.
또한 직접 담을 수도 있다.
var 변수명 = ["내용","내용1","내용2"]로 담는다 .
