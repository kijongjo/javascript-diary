javascript에는 사용자에게 직접 정보를 입력받아 처리하는 명령어가 존재한다.

prompt명령이다. 
특이점은 prompt에 수를 입력해도 수로 받아 들이지 않고 string으로 받아들인다는 것이다.
때문에 1+1을 해도 2가 아닌 11이 된다. 이를 해결하기 위해서 형변환을 해주어야한다. 

Number(prompt("입력값:))을 해주고 더해준다. 

*alert명령어 
결과값이나 어떠한 정보를 사용자혹은 문서에 전달할때 알림창으로 전달하는 방법이다. 


